<zk>
    <scrollview vflex="1" hflex="1">
        <zk forEach="1,2,3,4,5">
            <window title="window ${each}" border="normal" width="100%" height="300px">
                This is Window ${each}
            </window>
        </zk>
    </scrollview>
</zk>