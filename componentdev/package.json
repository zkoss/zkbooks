{
  "name": "zk-componentdev-ts",
  "version": "1.0.0",
  "private": true,
  "description": "TypeScript authoring track for ZK Component Development Essentials",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "watch": "tsc -p tsconfig.json --watch",
    "type-check": "tsc -p tsconfig.json --noEmit"
  },
  "devDependencies": {
    "typescript": "5.3.3",
    "zk-types": "10.3.0"
  }
}
